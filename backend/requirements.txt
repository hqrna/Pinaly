# --- FastAPI 必須 ---
fastapi
uvicorn
python-dotenv
python-multipart
requests
httpx
supabase
pydantic-settings
email-validator

# --- GeoCLIP / AI 関連 (必要最小限) ---
# バージョン指定を外すことで、Docker環境に合うものを自動選択させます
numpy
Pillow
torch --index-url https://download.pytorch.org/whl/cpu
# ※ GPUを使う場合は --index-url ... の行を削除して torch だけにする

# その他必要なツール
tqdm